import { Box } from '@mui/material';
import Layout from '../components/Layout';
import Card from '../cards/CardSobre';

function Sobre() {

    return (
        <Layout>
            <Box>
                <Card />
            </Box>
        </Layout>
    );
}

export default Sobre;